import React from 'react'

const Meduim = (props) => {
  return (
    <div>
      <p>Name : {props.name}</p>
      <p>Age : {props.age}</p>
      <p>Course: {props.course}</p>
    </div>
  )
}

export default Meduim
